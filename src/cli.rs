//! CLI surface: clap v4 (derive) definitions for `up`/`attach`/`ls`/`down`/
//! `prune`/`config`/`doctor` and their flags, plus dispatch into `commands`.
//!
//! See backlog CLI-01.

// TODO(CLI-01): clap `Cli`/`Command` enums + flags (-d/--detach, --branch,
//               --force/-f, --idle); dispatch to the matching command.

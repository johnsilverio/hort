//! `ls`: reconcile (kernel + metadata + worktrees) and render; never mutates,
//! surfaces inconsistencies as information (PRD §3.3).
//!
//! See backlog C-02.

// TODO(C-02): the LsCommand coordinator.

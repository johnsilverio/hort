//! `attach <name>`: join a new session to a live sandbox's namespaces via the
//! tenant API. Never creates; distinct `not-running` vs `absent` errors.
//!
//! See backlog C-04.

// TODO(C-04): the AttachCommand coordinator.

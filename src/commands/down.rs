//! `down <name>`: execute the teardown plan in the mandatory order; confirm if
//! sessions are open (`--force` skips); refuse without a TTY. no-git keeps the
//! user's folder (PRD §3.4).
//!
//! See backlog C-03.

// TODO(C-03): the DownCommand coordinator.

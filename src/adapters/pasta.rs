//! The `pasta` external-binary wrapper: the userspace bridge from the sandbox
//! netns to the host (connectivity, not a firewall — PRD §6.3). Stub until SP-1.
//!
//! See backlog A-S2 (gated).

// TODO(A-S2): spawn/teardown pasta with the spike-pinned flags.

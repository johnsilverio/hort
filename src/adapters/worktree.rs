//! git `WorktreeProvider`: `git worktree add/list/prune`, `git status`
//! dirty/clean, branch creation/checkout. Shells the system `git`; not a lib.
//!
//! See backlog A-04.

// TODO(A-04): the git-backed worktree provider + temp-repo contract test.

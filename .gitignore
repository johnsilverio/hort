# --- Rust ---
/target/
**/*.rs.bk
# hort is a binary crate → KEEP Cargo.lock committed (do not ignore it).

# --- Private design & local tooling (kept OUT of the public repo) ---
/docs/
/.claude/
CLAUDE.md mirrors the private design decisions. Uncomment to keep it private too:
/CLAUDE.md

# --- OS / editor noise ---
.DS_Store
*.swp
*~
